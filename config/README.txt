Global -> settings that tend to stay the same until a major software revision or emergency fix occurs
Environment -> Local to VM, e.g. deployment-specific
Instance -> Specific to a scenario, e.g. a given developer's check-out, new scenario (client), different database instance etc
